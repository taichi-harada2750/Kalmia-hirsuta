# Kalmia-hirsuta UI 使用方法 (操作マニュアル)

このドキュメントでは、Kalmia-hirsutaのUIにおける基本的な操作方法とインタラクションの仕組みについて解説します。

## 1. 基本となる「手」の認識
本システムはカメラを用いてユーザーの両手を認識します。
特別なコントローラーを持つ必要はなく、カメラの前に手をかざすだけで画面上にカーソル（左右の手それぞれに対応）が現れ、手の動きに合わせて追従します。

## 2. コア・アクション：「握る（Grab）」
UIを操作するための最も基本的なアクションは**「手を握る（Grab）」**動作です。
指を曲げて手を握り込むジェスチャーが、従来のマウスにおける「左クリック」や「ドラッグ」に相当します。

## 3. 各UI要素の操作方法

### アイコンのクリック（選択）
*   **ホバー（Hover）**: 画面上のアイコンやボタンに手を（カーソルを）重ねると、UIが反応してアニメーション（鼓動のようなエフェクト）を開始します。
*   **クリック（Click）**: アイコンにホバーした状態で**「手を握る」**と、そのアイコンが選択・実行されます。

### リングUI（円形メニュー）の呼び出し
*   **サモン（Summon）**: 画面上に配置された「呼び出しトリガー（Summon Button）」に手をかざし、**「手を握る」**ことで、アプリ一覧が表示されるリング状のメニュー（Ring UI）が展開されます。
    *   ※デバッグ・補助用途として、キーボードの `Space` キーでも呼び出しが可能です。

### リングUIのスクロール
*   **スクロール（Scroll）**: リングUIが表示されている状態で、何もない空間で**「手を握りながら、手を左右に動かす」**ことで、リングUI全体が回転（スクロール）します。
*   手をパーに戻す（握るのをやめる）と、スクロールが停止します。

## 4. マウスによる代替操作（デバッグ用）
カメラが利用できない環境やデバッグ時のために、マウスでも同様の操作が可能です。
*   **カーソル移動**: マウスポインタの移動がそのまま手の移動として扱われます。
*   **握る（Grab）**: マウスの**「左クリック」**を押している間が、手を握っている状態として判定されます。

---
この直感的な「かざす」「握る」「動かす」という3つの動作を組み合わせることで、すべての機能にアクセスできるように設計されています。
